[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = backend/tests modules scripts/tests
pythonpath = backend scripts
filterwarnings =
    ignore::DeprecationWarning:google._upb._message
